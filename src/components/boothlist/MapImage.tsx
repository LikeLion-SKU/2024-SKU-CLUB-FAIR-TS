import ImageCard from '../common/image/ui/ImageCard';

export default function MapImage() {
  return (
    <div id="mapImage" className="w-full p-[40px]">
      <ImageCard dirName="boothlist" imageName="0" imageFormat="svg" alt="map-image" />
    </div>
  );
}
